{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "semanticCommits": "enabled",
  "semanticCommitScope": "deps",
  "timezone": "Asia/Tokyo",
  "schedule": ["after 9am and before 10am on monday"],
  "minimumReleaseAge": "7 days",
  "prConcurrentLimit": 10,
  "labels": ["dependencies"],
  "reviewers": ["genzouw"],
  "vulnerabilityAlerts": {
    "description": [
      "脆弱性修正は minimumReleaseAge と schedule の対象外にして即時 PR にする。",
      "Dependabot の cooldown が security updates に効かなかったのと同じ挙動を保つため。"
    ],
    "minimumReleaseAge": null,
    "schedule": [],
    "addLabels": ["security"]
  },
  "packageRules": [
    {
      "description": [
        "依存更新はすべて chore プレフィックスで出す。",
        "config:recommended に含まれる :semanticPrefixFixDepsChoreOthers は本番依存を fix にするが、",
        "fix は release 生成ツールがリリース対象として扱うため、Dependabot 時代と同じ chore に固定する。"
      ],
      "matchPackageNames": ["*"],
      "semanticCommitType": "chore"
    },
    {
      "description": "GitHub Actions の更新のプレフィックスとラベル (Dependabot の commit-message.prefix と同じ)",
      "matchManagers": ["github-actions"],
      "semanticCommitType": "ci",
      "addLabels": ["github-actions"],
      "prConcurrentLimit": 5
    },
    {
      "description": "npm / bun の更新には npm ラベルを付ける",
      "matchManagers": ["npm", "bun"],
      "addLabels": ["npm"]
    },
    {
      "description": "eslint 本体とプラグイン/設定はバージョンを揃える必要があるため 1 PR にまとめる (Dependabot の groups.eslint と同じ)",
      "matchManagers": ["npm"],
      "matchPackageNames": ["eslint", "eslint-*", "@eslint/**", "globals"],
      "groupName": "eslint"
    }
  ]
}
